import { PagePlaceholder } from "@/app/_components/PagePlaceholder";

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="오늘 매장"
      plan="통계와 주문 표는 prototype/dashboard.html 을 따라 채운다."
    />
  );
}

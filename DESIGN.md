# 디자인 시스템

**흰 캔버스 위의 관리자 도구.** 색은 데이터가 쓰고 크롬은 비켜선다.
검정에 가까운 남색 하나가 모든 액션을 맡고, 파스텔은 상태를 구분하는 데만 쓴다.

출처는 `prototype/dashboard.html` 이다. **시안이 원본이고 이 문서가 요약이다.**
둘이 어긋나면 시안을 믿고 이 문서를 고친다.

토큰은 `src/app/globals.css` 의 `@theme inline` 에 등록돼 있어 Tailwind 유틸리티로 바로 쓴다.
새 색이나 반경이 필요하면 **여기 표에 먼저 추가하고** `globals.css` 에 등록한 뒤 쓴다.

---

## 색

### 브랜드와 액션

| 토큰 | 값 | Tailwind | 쓰임 |
|---|---|---|---|
| `primary` | `#050038` | `bg-primary` | 주 버튼, 활성 네비, 다크 카드. **모든 주요 액션이 이 색이다** |
| `brand-yellow` | `#ffd02f` | `bg-brand-yellow` | 워드마크 사각형, 다크 카드 안 아이콘. **버튼에 쓰지 않는다** |
| `brand-blue` | `#4262ff` | `text-brand-blue` | 인라인 링크, 포커스 링 |
| `blue-deep` | `#2f4ae0` | `text-blue-deep` | 라벤더 배경 위 파랑 글자. 대비 확보용 |

**노랑은 브랜드 표식이지 액션이 아니다.** 버튼이나 넓은 배경에 쓰면 시스템이 무너진다.
액션은 전부 `primary` 다.

### 표면

| 토큰 | 값 | Tailwind | 쓰임 |
|---|---|---|---|
| `canvas` | `#ffffff` | `bg-canvas` | 페이지 바탕, 패널 바탕 |
| `surface` | `#f5f5f7` | `bg-surface` | 호버, 검색 입력 바탕 |
| `surface-soft` | `#fafafb` | `bg-surface-soft` | 표 헤더, 아주 옅은 구분 |
| `hairline` | `#e6e6e9` | `border-hairline` | 기본 1px 경계 |
| `hairline-soft` | `#f0f0f2` | `border-hairline-soft` | 표 행 구분선 |
| `hairline-strong` | `#c3c2cf` | `border-hairline-strong` | 보조 버튼 테두리, 비활성 아이콘 |

### 글자

| 토큰 | 값 | Tailwind | 대비(흰 배경) | 쓰임 |
|---|---|---|---|---|
| `ink` | `#050038` | `text-ink` | 19.4:1 | 제목, 본문 |
| `slate` | `#45455c` | `text-slate` | 9.3:1 | 표 안 숫자, 보조 본문 |
| `steel` | `#6e6e82` | `text-steel` | 5.0:1 | 라벨, 메타, 캡션 |
| `on-dark` | `#ffffff` | `text-on-dark` | | 다크 표면 위 글자 |
| `on-dark-muted` | `rgba(255,255,255,.68)` | `text-on-dark-muted` | 9.0:1 | 다크 표면 위 보조 글자 |

**회색은 이 셋뿐이다.** `steel` 보다 옅은 회색을 새로 만들지 않는다. AA 4.5:1 이 깨진다.

### 파스텔

배경과 글자가 짝이다. **짝을 바꾸지 않는다.** 대비를 맞춰 고른 조합이다.

| 배경 | 값 | 글자 | 값 | 대비 | 쓰임 |
|---|---|---|---|---|---|
| `coral-light` | `#ffe2dc` | `coral-dark` | `#8a2140` | 7.2:1 | 경고, 교환·반품 |
| `teal-light` | `#d9f2ee` | `moss-dark` | `#14524a` | 7.6:1 | 완료, 배송 중 |
| `yellow-light` | `#fff3c4` | `yellow-dark` | `#6b5a00` | 6.1:1 | 대기, 준비 중 |
| `lavender` | `#efebff` | `blue-deep` | `#2f4ae0` | 5.7:1 | 정보, 결제 완료 |
| `rose-light` | `#ffe0ef` | `coral-dark` | `#8a2140` | 6.9:1 | 실패, 동기화 오류 |

### 상태

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `success` | `#1f9e5e` | 증가 지표 |
| `danger` | `#c93b3b` | 감소 지표, 알림 배지 |

---

## 타이포

| 역할 | 서체 | Tailwind |
|---|---|---|
| 디스플레이 | Outfit + Pretendard | `font-display` |
| 본문·UI | Pretendard | `font-sans` (기본) |
| 숫자·코드 | JetBrains Mono + D2Coding | `font-mono` |

Outfit 은 라틴 글자만 커버한다. **한글은 Pretendard 로 떨어진다.** 스택 순서가 그렇게 짜여 있다.
`font-display` 는 제목과 숫자에만 쓴다. 본문에 쓰면 한글 구간에서 차이가 안 보이고 로드만 늘어난다.

### 스케일

| 쓰임 | 크기 | 굵기 | 자간 | 서체 |
|---|---|---|---|---|
| 페이지 제목 | 28px | 500 | -0.015em | display |
| 통계 숫자 | 32px | 500 | -0.02em | display |
| 패널 제목 | 17px | 500 | 0 | display |
| 워드마크 | 16px | 600 | -0.01em | display |
| 네비 항목 | 15px | 500 | 0 | sans |
| 본문 · 버튼 · 입력 | 14px | 400~500 | 0 | sans |
| 캡션 · 메타 | 13px | 400 | 0 | sans |
| 섹션 라벨 | 11px | 600 | 0.5px | sans |
| 표 숫자 | 13~14px | 400 | 0 | mono |

**굵기는 400 / 500 / 600 셋뿐이다.** 700 을 쓰지 않는다.
강조는 굵기가 아니라 색(`ink` vs `steel`)과 크기로 만든다.

**한글 조판 3종은 전역에 걸려 있다.** `word-break: keep-all`, `line-height: 1.6`,
음수 자간은 디스플레이 크기에서만. 개별 컴포넌트에서 다시 지정하지 않는다.

**자릿수가 맞아야 읽히는 값에는 `tabular-nums` 를 준다.** 금액, 수량, SKU, 통계 숫자.
`.num` `.amt` 같은 표 셀은 `font-mono` 와 함께 쓴다.

---

## 간격

**8px 기준.** Tailwind 기본 스케일(`--spacing: 0.25rem`)이 그대로 맞아떨어져서 별도 토큰을 두지 않는다.

| 시안 | Tailwind | 쓰임 |
|---|---|---|
| 4px | `1` | 아이콘과 라벨 사이 |
| 8px | `2` | 촘촘한 묶음 |
| 12px | `3` | 컨트롤 안쪽, 사이드바 좌우 |
| 16px | `4` | 패널 헤더, 모바일 여백 |
| 20px | `5` | 패널 본문, 다크 카드 |
| 24px | `6` | 상단바 좌우, 파스텔 카드 |
| 32px | `8` | 큰 구획 |
| 40px | `10` | 섹션 사이 |

---

## 모서리

**섞이지 않게 이름으로 못 박았다.** Tailwind 기본 `rounded-lg` 같은 걸 쓰지 않는다.

| 토큰 | 값 | Tailwind | 쓰임 |
|---|---|---|---|
| `note` | 4px | `rounded-note` | 스티키노트. 종이처럼 거의 각지게 |
| `field` | 8px | `rounded-field` | 입력, 네비 행, 아이콘 버튼 |
| `card` | 12px | `rounded-card` | 작은 카드 |
| `panel` | 16px | `rounded-panel` | 패널, 다크 카드, 표 컨테이너 |
| `pastel` | 28px | `rounded-pastel` | 파스텔 카드 |
| 원형 | `9999px` | `rounded-full` | **모든 버튼과 배지**, 아바타 |

**버튼은 예외 없이 pill 이다.** 각진 버튼은 이 시스템에 없다.

---

## 그림자

거의 안 쓴다. 깊이는 **경계선과 배경색**이 만든다.

| 토큰 | 값 | Tailwind | 쓰임 |
|---|---|---|---|
| `note` | `0 4px 12px rgba(5,0,56,.06)` | `shadow-note` | 스티키노트 기본 |
| `lift` | `0 12px 32px -4px rgba(5,0,56,.08)` | `shadow-lift` | 스티키노트 호버 |

패널, 표, 파스텔 카드에는 **그림자를 넣지 않는다.** 1px `hairline` 이면 충분하다.

---

## 레이아웃

```
┌──────────┬──────────────────────────────────┐
│ side     │ topbar  60px (sticky)            │
│ 232px    ├──────────────────────────────────┤
│ (sticky) │ content                          │
│          │   pagehead                       │
│ 워드마크  │   stats  4칸                     │
│ 네비     │   split  2fr / 1fr               │
│ ...      │   panel  (주문 표)               │
│ 계정     │   pastels  2칸                   │
└──────────┴──────────────────────────────────┘
```

사이드바 `232px`, 상단바 `60px`. 둘 다 sticky 다.
콘텐츠는 최대 폭을 두지 않고 화면을 채운다. 표가 주인공이라 넓을수록 낫다.

### 중단점

| 폭 | 바뀌는 것 |
|---|---|
| ~1279px | 2열 그리드가 1열로 |
| ~1023px | **사이드바가 상단 가로 네비로 눕는다.** 통계 4칸 → 2칸, 파스텔 1칸 |
| ~767px | 상단바 검색 숨김, 통계 1칸, 제목 24px, 기간 탭이 가로 꽉 참 |

1023px 에서 사이드바가 사라지지 않고 **가로로 눕는다.** 관리자 화면에 햄버거를 넣지 않는다.
탭 하나 줄이자고 메뉴를 한 겹 숨기면 이동이 매번 두 번이 된다.

---

## 컴포넌트

### 버튼

높이 38px, 좌우 20px, `rounded-full`, 14px / 500. `sm` 은 34px / 좌우 14px / 13px.

| 변형 | 배경 | 글자 | 쓰임 |
|---|---|---|---|
| `primary` | `primary` | 흰색 | 주 액션. **화면당 하나가 원칙** |
| `secondary` | 흰색 + `hairline-strong` 테두리 | `ink` | 보조 액션 |
| `on-dark` | 흰색 | `primary` | 다크 카드 안 |

비활성은 `hairline` 배경 + `steel` 글자. `:active` 에서 `scale(.98)`.

### 패널

`bg-canvas` + `hairline` 테두리 + `rounded-panel`. 헤더와 본문이 `hairline-soft` 로 나뉜다.
헤더는 제목(17px display) + 설명(13px steel) + 우측 링크.

**패널 안에 패널을 넣지 않는다.** 중첩이 필요하면 구획이 잘못 나뉜 것이다.

### 스티키노트

파스텔 배경 + 짝 글자색, `rounded-note`(4px), `shadow-note`, 최소 높이 112px.
`nth-child` 로 -1.4deg ~ 1.3deg 씩 기울어져 있다. 호버에서 수평으로 펴지며 떠오른다.

**이 화면의 시그니처다.** 다른 곳에 복제하지 않는다. 하나뿐이라 눈에 남는다.

### 통계 행

4칸. **카드가 아니다.** 배경 없이 `hairline` 위아래 선과 칸 사이 `hairline-soft` 세로선만 있다.
라벨(13px steel) → 숫자(32px display) → 증감(13px, success/danger/steel).

로딩 중에는 숫자 자리에 `surface` 색 블록이 깜빡인다.

### 파스텔 카드

`rounded-pastel`(28px), 안쪽 24px. 배경과 글자는 위 파스텔 표의 짝을 쓴다.
안에 들어가는 버튼은 `primary` 다. 파스텔 위에서도 액션 색은 바뀌지 않는다.

### 다크 카드

`bg-primary` + 흰 글자 + `rounded-panel` + 안쪽 20px.
안에서 아이콘만 `brand-yellow` 를 쓴다. 노랑이 액션이 아닌 표식으로 쓰이는 유일한 자리다.

### 배지

`rounded-full`, 3px 10px, 13px / 600. 상태별로 파스텔 짝을 쓴다.

| 상태 | 짝 |
|---|---|
| 결제 완료 | lavender / blue-deep |
| 배송 준비 | yellow-light / yellow-dark |
| 배송 중 | teal-light / moss-dark |
| 교환·반품 | coral-light / coral-dark |

### 표

헤더는 `surface-soft` 배경 + 11px / 600 / 0.5px 자간 / steel.
행 구분은 `hairline-soft` **아래쪽만**. 위아래 둘 다 긋지 않는다.
숫자 열은 `font-mono` + `tabular-nums`, 금액은 우측 정렬.

가로로 넘치면 표만 스크롤한다. 페이지 본문은 가로 스크롤되지 않는다.

### 목록 화면

대시보드를 뺀 목록 화면은 전부 같은 세 줄이다.

```
PageTitle     28px 제목
SearchPanel   검색 표 + 오른쪽 정렬 버튼
DataGrid      총 N건 + ag-grid
```

**검색 영역은 표다.** 한 줄에 조건 둘. `<tr> <th>라벨</th><td>칸</td> <th>라벨</th><td>칸</td> </tr>`.

```
┌──────────┬────────────────────┬──────────┬────────────────────┐
│ 제품명    │ [ 제품명으로 찾기  ] │ 카테고리  │ [ 예: 글러브       ] │
├──────────┼────────────────────┼──────────┴────────────────────┤
│ 상태      │ [ 전체           v ] │                               │
└──────────┴────────────────────┴───────────────────────────────┘
                                                 ( 초기화 )  ( 검색 )
```

- 바깥은 `rounded-panel` + `hairline`. 줄 사이와 두 번째 쌍 앞은 `hairline-soft` 1px
- `th` 는 140px(1023px 이하 112px), `surface` 바탕, 13px / 500 `slate`. 라벨이 칸과 연결돼 누르면 칸으로 간다
- `td` 는 좌우 12px 위아래 10px. 칸은 셀 폭을 채운다
- 조건이 홀수면 마지막 줄 오른쪽 쌍 자리는 비워 둔다
- 767px 이하에서는 한 줄에 `th`(96px) `td` 한 쌍씩 내려온다

입력칸과 셀렉트는 38px `rounded-field` 로 같은 모양이다. 공통 클래스는 `components/ui/field-class.ts`.
글자칸에는 아이콘을 넣지 않는다. 셀렉트만 오른쪽에 `CaretDown` 14px `steel`.
브라우저 기본 셀렉트 화살표와 검색칸 지우기 버튼은 끈다.

| 상태 | 바탕 | 테두리 | 글자 |
|---|---|---|---|
| 비어 있음 | `surface` | `hairline` | 자리 표시는 `steel`. 셀렉트의 '전체'도 `steel` |
| 호버 | 그대로 | `hairline-strong` | |
| **조건 걸림** | `canvas` | `hairline-strong` | `ink` |
| 포커스 | `canvas` | `brand-blue` + 1px 링 | `ink` |
| 비활성 | `hairline-soft` | `hairline-soft` | `steel` |

**조건 걸림을 빈 칸과 다르게 그린다.** 목록을 보다가 어떤 조건이 걸려 있는지 칸만 훑어도 알 수 있어야 한다.
포커스 링은 한 겹이다. 전역 `:focus-visible` 윤곽선은 `@layer base` 에 있어서 입력칸이 `outline-hidden` 으로 덮는다.

버튼은 표 아래 오른쪽 끝에 초기화(secondary)와 검색(primary). 767px 이하에서는 두 버튼이 폭을 반씩 나눈다.
조건은 GET 으로 URL 에 남는다. 클라이언트 state 로 들고 있지 않는다.

**표는 ag-grid 다.** `components/common/DataGrid.tsx` 의 Quartz 테마가 위 표 규칙을 옮긴 것이다.
헤더 38px, 행 48px, 칸 좌우 20px, 칸 사이 세로줄 없음, 바깥 모서리 16px.
서식은 컬럼 `type` 셋으로만 고른다.

| type | 모양 |
|---|---|
| `number` | 우측 정렬, `font-mono` + `tabular-nums`, 천 단위 콤마 |
| `date` | `2026.09.02`, 13px mono `slate` |
| `badge` | 파스텔 짝 배지. 짝이 없는 값은 `surface` / `slate` 중립 배지 |

비어 있으면 표 안에 돋보기 아이콘 + 무엇이 없는지 + 다음에 할 것을 띄운다.
검색 조건이 걸려 있을 때와 아예 없을 때 문구를 나눈다.

**페이지 이동은 ag-grid 기본 패널을 끄고 `components/ui/Pagination.tsx` 로 그린다.**
그리드가 페이지를 들고 있고, 이 컴포넌트는 그리드가 알려 준 값을 그리고 누르면 그리드 API 로 옮긴다.

```
페이지당 [20 v]            «  ‹  1  2 (3)  ›  »
```

- 표 아래 8px. 페이지 크기 선택은 왼쪽, 번호는 가운데
- 번호는 현재 페이지를 가운데 두고 다섯 개까지. 36px 원형, 현재는 `primary` 바탕 흰 글자, 나머지 `slate` + 호버 `surface`
- 처음·이전·다음·마지막은 같은 36px 원형 아이콘 버튼. 갈 곳이 없으면 `hairline-strong` 으로 비활성
- 페이지 크기 선택은 검색 영역과 같은 `fieldClass` 셀렉트, 폭 84px. 20 / 50 / 100
- 행이 없어도 1 페이지 하나는 그린다
- 767px 이하에서는 번호 줄이 위, 페이지 크기 선택이 아래로 가운데 정렬

---

## 상태

**모든 목록과 폼은 다섯 상태를 갖춘다.** 하나라도 빠지면 미완성이다.

| 상태 | 처리 |
|---|---|
| 기본 | |
| 호버 | `surface` 배경. 전환 160ms |
| 로딩 | 최종 레이아웃 모양의 스켈레톤. 스피너를 쓰지 않는다 |
| 비어 있음 | 아이콘 + 무엇이 없는지 + 다음에 할 것 |
| 오류 | 무엇이 실패했는지 + 복구 수단. 인라인으로 |

비활성 컨트롤도 상태다. 조건이 안 맞으면 숨기지 말고 비활성으로 둔다.

---

## 모션

전환 **160ms**, 이징 `cubic-bezier(.2,.8,.3,1)` (`ease-brand`).

움직이는 것은 셋뿐이다.

- 호버 배경색
- 버튼 누름 `scale(.98)`
- 스티키노트 떠오름

**`prefers-reduced-motion: reduce` 에서 전부 멈춘다.** 전역에 걸려 있다.

---

## 하지 않는 것

- **노랑을 버튼이나 큰 배경에 쓰지 않는다.** 워드마크와 다크 카드 아이콘 전용이다
- **`steel` 보다 옅은 회색을 만들지 않는다.** AA 4.5:1 이 깨진다
- **파스텔 배경과 글자 짝을 바꾸지 않는다.** 대비를 맞춰 고른 조합이다
- **각진 버튼을 만들지 않는다.** 전부 pill 이다
- **패널·표·파스텔에 그림자를 넣지 않는다.** 스티키노트만 뜬다
- **네 번째 파스텔 계열을 늘리지 않는다.** 다섯 개가 이미 상태를 다 덮는다
- **관리자 화면에 햄버거 메뉴를 넣지 않는다.** 태블릿에서는 가로로 눕힌다
- **폰트 굵기 700 을 쓰지 않는다**

## 아직 정하지 않은 것

- **다크 모드.** 지금은 라이트 전용으로 확정했다. 파스텔 짝이 다크에서 통째로 다시 필요해서
  범위를 나눴다. 필요해지면 파스텔 표를 먼저 다시 짠다
- **차트 팔레트.** 파스텔은 상태용이라 계열 구분에 그대로 못 쓴다
